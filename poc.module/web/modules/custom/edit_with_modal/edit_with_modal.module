<?php

/**
* Implements hook_preprocess_html().
*/

function edit_with_modal_preprocess_html(&$variables) {
  if (!isset($variables['#attached'])) return;
  // $variables['#attached'][] = ['library'];
  $variables['#attached']['library'][] = 'olivero_umeki/global-styling';
}
